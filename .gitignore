node_modules/
dist/
build/
*.log
.env
.env.local
.DS_Store
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
coverage/
.vite/

# TypeScript incremental build state
*.tsbuildinfo

# Scratch from local agent-API testing: a session token and a signed mandate.
.agent-token
.agent-mandate

# A competitor's source, downloaded for comparison. Not ours to publish.
AgentPay-main.zip
